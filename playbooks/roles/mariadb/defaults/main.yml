mariadb_version: # null is default, "latest" is latest stable, "x.y" is specific version
mariadb_source: pkg
mariadb_devel: false
mariadb_libs: []
mariadb_gems: []
mariadb_charset: utf8mb4
mariadb_collation: utf8mb4_general_ci
