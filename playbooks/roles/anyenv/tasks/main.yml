- name: Task install
  ansible.builtin.import_tasks:
    file: install.yml

- name: Task env
  ansible.builtin.include_tasks:
    file: env.yml
  loop: '{{ anyenv_envs }}'
  loop_control:
    loop_var: anyenv_env
