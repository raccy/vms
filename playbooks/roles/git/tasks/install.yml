- name: Task install on app for git
  ansible.builtin.include_role:
    name: app
    tasks_from: install
  vars:
    app_install:
      name: git
      pkgs: '{{ git_libs | map("regex_replace", "^", "git-") }}'
