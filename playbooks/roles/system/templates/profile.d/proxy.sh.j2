# proxy
{% for env in proxy_env | dict2items %}
export {{ env.key | upper }}="{{ env.value }}"
{% endfor %}
{% for env in proxy_env | dict2items %}
export {{ env.key }}="{{ env.value }}"
{% endfor %}
