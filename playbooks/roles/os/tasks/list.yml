- name: Set os_list
  ansible.builtin.set_fact:
    os_list: >-
      {{
        os_list_os[os_distro_version] | default([])
        | union(os_list_os[os_distro] | default([]))
        | union(os_list_os[os_family] | default([]))
      }}
