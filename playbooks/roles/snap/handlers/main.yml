- name: Restart snapd service
  become: true
  ansible.builtin.service:
    name: snapd
    state: restarted
  when:
    - ansible_facts.service_mgr == "systemd"
    - snap_auto
  listen: snap_restart_snapd
